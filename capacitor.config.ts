import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'FooterWithTextInSheetModal',
  webDir: 'dist'
};

export default config;
